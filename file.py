file = open("data.txt", "r")  # modes: 'r', 'w', 'a', 'r+', 'w+', 'a+'

📌 Common Modes:

'r' → Read (default), file must exist

'w' → Write, overwrites file

'a' → Append to end

'r+' → Read + Write, doesn't truncate

'w+' → Write + Read, truncates file

'a+' → Append + Read 

